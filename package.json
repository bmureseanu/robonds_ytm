{
  "name": "bvb-ytm",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p .",
    "build:data": "tsx src/build-data.ts",
    "build:data:prod": "node dist/build-data.js",
    "cli": "tsx src/cli.ts",
    "serve": "npx --yes http-server src/public -p 3000 -c-1",
    "typecheck": "tsc -p . --noEmit"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "cheerio": "^1.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "tsx": "^4.19.0",
    "typescript": "^5.4.0"
  }
}
